extends layout.pug 

block appContent 
    h1 Hamza Choudhury
    h2 You have: #{money}
    if !recentTransaction
      p No recent transcations.
    else 
      p Recent transaction.
    button Logout